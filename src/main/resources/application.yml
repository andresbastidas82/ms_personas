server:
  port: 8093

spring:
  r2dbc:
    url: r2dbc:mysql://localhost:3306/persons_db?serverTimezone=UTC
    username: root
    password: 123456

  sql:
    init:
      mode: always

services:
  technology:
    base-url: http://localhost:8090/api/v1
